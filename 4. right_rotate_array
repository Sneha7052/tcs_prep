#include <iostream>
using namespace std;
int main() {
	int n;
	cin>>n;
	int nums[n];
	for(int i=0;i<n;i++){
	    cin>>nums[i];
	}
	int temp=nums[n-1];
  for(int i=n-1;i>=0;i--){ //if we do forwad loop then orginal data will be cpverted to new data
     nums[i]=nums[i-1];
  }
  nums[0]=temp;
  for(int i=0;i<n;i++){
      cout<<nums[i]<<" ";
}
return 0;
}
